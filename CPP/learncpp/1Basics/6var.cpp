#include <iostream>

int main(){
    // This stores some random values (which is different on each run)
    int x;

    // This stores 0 in x
    // int x{};
    std::cout << x << "\n";
    return 0;
}